import createComponentExample from '../../../lib';
import { Flip } from 'tiny-ui/src';

export default createComponentExample({
    componentName: 'flip',
    examples: ['basic', 'direction'],
    dependencies: { Flip },
});

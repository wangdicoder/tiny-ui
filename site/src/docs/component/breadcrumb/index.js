import createComponentExample from '../../../lib';
import { Breadcrumb, Icon } from 'tiny-ui/src';

export default createComponentExample({
    componentName: 'breadcrumb',
    examples: ['basic', 'icon'],
    dependencies: { Breadcrumb, Icon },
});

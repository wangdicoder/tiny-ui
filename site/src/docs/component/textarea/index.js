import createComponentExample from '../../../lib';
import { Textarea } from 'tiny-ui/src';

export default createComponentExample({
    componentName: 'textarea',
    examples: ['basic', 'count'],
    dependencies: { Textarea },
});

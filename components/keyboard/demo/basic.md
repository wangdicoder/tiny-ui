<demo>

### Basic

A simple usage.

```jsx live
() => {
  return (
    <div>
      Using <Keyboard>command</Keyboard> + <Keyboard>option</Keyboard> + <Keyboard>i</Keyboard> to open "Developer Tool".
    </div>
  );
}
```

</demo>

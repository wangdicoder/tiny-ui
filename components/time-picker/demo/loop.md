<Demo>

### Infinite Scroll

Enable circular scrolling so columns wrap around continuously.

```jsx live
() => {
  return (
    <TimePicker
      loop
      placeholder="Loop scroll"
    />
  );
}
```

</Demo>
